services:
  - type: web
    name: growthify-backend
    env: node
    region: oregon
    plan: free
    branch: main
    root: Backend
    buildCommand: npm install
    startCommand: npm run start
    autoDeploy: true
